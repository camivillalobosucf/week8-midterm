// localStorage helper functions — placeholders for Week 8

/**
 * Retrieve entries from localStorage by key.
 */
export function getEntries(key) {
  // TODO: parse JSON from localStorage and return array
  return []
}

/**
 * Save entries array to localStorage under the given key.
 */
export function saveEntries(key, entries) {
  // TODO: stringify and store entries
}

/**
 * Add a single entry to an existing localStorage array.
 */
export function addEntry(key, entry) {
  // TODO: get current entries, push new one, save back
}

/**
 * Delete an entry by its id from the localStorage array.
 */
export function deleteEntry(key, id) {
  // TODO: filter out entry with matching id, save back
}

/**
 * Update an existing entry by its id.
 */
export function updateEntry(key, id, updatedFields) {
  // TODO: map over entries, merge updated fields, save back
}
